apiVersion: v1
kind: Service
metadata:
  name: whoami-external
spec:
  type: NodePort
  selector:
    app: whoami
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007
      protocol: TCP
      name: http
